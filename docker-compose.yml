version: '2'
services:
  app:
    container_name: comments
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - XDEBUG_MODE=coverage
    restart: always
    volumes:
      - ./:/var/www
